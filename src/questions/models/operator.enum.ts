export enum EasyOperator {
    ADDITION = "+",
    SUBTRACTION = "-",
}

export enum HardOperator {
    ADDITION = "+",
    SUBTRACTION = "-",
    MULTIPLICATION = "*",
    DIVISION = "/",
}

export enum VeryHardOperator {
    ADDITION = "+",
    SUBTRACTION = "-",
    MULTIPLICATION = "*",
    DIVISION = "/",
    MODULUS = "%"
}